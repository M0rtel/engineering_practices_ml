#!/bin/bash
# Скрипт для создания виртуального окружения

set -e

echo "🐍 Создание виртуального окружения..."

# Проверка наличия UV
if ! command -v uv &> /dev/null; then
    echo "❌ UV не установлен. Устанавливаю..."
    curl -LsSf https://astral.sh/uv/install.sh | sh
    export PATH="$HOME/.local/bin:$PATH"
fi

# Создание виртуального окружения
if [ -d ".venv" ]; then
    echo "⚠️  Виртуальное окружение уже существует в .venv/"
    read -p "Пересоздать? (y/N): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        echo "🗑️  Удаляю старое окружение..."
        rm -rf .venv
        uv venv
        echo "✅ Виртуальное окружение пересоздано"
    else
        echo "ℹ️  Оставляю существующее окружение"
    fi
else
    uv venv
    echo "✅ Виртуальное окружение создано в .venv/"
fi

echo ""
echo "📝 Следующие шаги:"
echo ""
echo "1. Активируйте виртуальное окружение:"
if [[ "$OSTYPE" == "msys" || "$OSTYPE" == "win32" ]]; then
    echo "   .venv\\Scripts\\activate"
else
    echo "   source .venv/bin/activate"
fi
echo ""
echo "2. Установите зависимости:"
echo "   uv sync"
echo ""
echo "Или используйте скрипт полной настройки:"
echo "   ./scripts/setup/setup.sh"
